# 🧪 TDD 문제 생성 자동화 구현 계획 (0824_plan3)

## 🎯 목표: 5가지 문제 유형 동시 구현

### 📋 **구현 문제 유형**
1. **Multiple Choice** (객관식) - 개념 이해 확인
2. **Short Answer** (주관식) - 설명 능력 평가
3. **Code Completion** (코드 완성) - 기존 시스템 개선
4. **Debug Code** (디버깅) - 실무 능력 향상
5. **True/False** (참/거짓) - 빠른 개념 확인

---

## 🏗️ Phase 1: 데이터 구조 설계 (수도코드)

### 1-1. 문제 유형별 데이터 스키마
```python
# 공통 베이스 구조
class BaseQuestion:
    id: str
    type: str  # "multiple_choice", "short_answer", "code_completion", "debug_code", "true_false"
    question: str
    topic: str
    difficulty: str  # "easy", "medium", "hard"
    estimated_time: int  # 예상 소요 시간 (초)
    learning_objectives: List[str]
    created_at: datetime
    ai_generated: bool = True

# 1. 객관식 문제
class MultipleChoiceQuestion(BaseQuestion):
    options: List[str]  # ["A) 선택지1", "B) 선택지2", ...]
    correct_answer: str  # "A", "B", "C", "D"
    explanation: str
    distractor_analysis: Dict[str, str]  # 오답 선택지별 설명

# 2. 주관식 문제  
class ShortAnswerQuestion(BaseQuestion):
    expected_keywords: List[str]
    sample_answer: str
    scoring_criteria: Dict[str, float]  # {"keyword_match": 0.4, "semantic": 0.6}
    min_length: int
    max_length: int

# 3. 코드 완성 문제
class CodeCompletionQuestion(BaseQuestion):
    code_template: str
    blanks: List[str]  # 정답 목록
    blank_hints: List[str]  # 힌트 목록
    test_cases: List[Dict]  # [{"input": [...], "output": ...}]
    language: str = "python"

# 4. 디버깅 문제
class DebugCodeQuestion(BaseQuestion):
    buggy_code: str
    errors: List[Dict]  # [{"line": 3, "error": "설명", "fix": "수정방법"}]
    corrected_code: str
    bug_types: List[str]  # ["syntax", "logic", "runtime"]

# 5. 참/거짓 문제
class TrueFalseQuestion(BaseQuestion):
    statement: str
    correct_answer: bool
    explanation: str
    common_misconception: str  # 자주 틀리는 이유
```

### 1-2. AI 프롬프트 템플릿 설계
```python
QUESTION_GENERATION_PROMPTS = {
    "multiple_choice": """
당신은 파이썬 프로그래밍 교육 전문가입니다.
다음 조건으로 객관식 문제를 생성해주세요:

**주제**: {topic}
**난이도**: {difficulty}
**학습 목표**: {learning_objectives}

**요구사항**:
- 명확하고 구체적인 질문
- 4개의 선택지 (A, B, C, D)
- 정답 1개, 그럴듯한 오답 3개
- 각 오답에 대한 설명
- 정답에 대한 상세 해설

**JSON 형식으로 응답**:
{{
    "question": "문제 내용",
    "options": ["A) 선택지1", "B) 선택지2", "C) 선택지3", "D) 선택지4"],
    "correct_answer": "A",
    "explanation": "정답 해설",
    "distractor_analysis": {{
        "B": "B를 선택하는 이유와 왜 틀렸는지",
        "C": "C를 선택하는 이유와 왜 틀렸는지", 
        "D": "D를 선택하는 이유와 왜 틀렸는지"
    }}
}}
""",

    "short_answer": """
당신은 파이썬 프로그래밍 교육 전문가입니다.
다음 조건으로 주관식 문제를 생성해주세요:

**주제**: {topic}
**난이도**: {difficulty}
**학습 목표**: {learning_objectives}

**요구사항**:
- 2-3문장으로 답할 수 있는 개념 설명 문제
- 핵심 키워드 3-5개 포함 필요
- 모범 답안 100-200자
- 채점 기준 명확화

**JSON 형식으로 응답**:
{{
    "question": "문제 내용",
    "expected_keywords": ["키워드1", "키워드2", "키워드3"],
    "sample_answer": "모범 답안 예시",
    "scoring_criteria": {{
        "keyword_match": 0.4,
        "semantic_similarity": 0.6
    }},
    "min_length": 50,
    "max_length": 300
}}
""",

    "code_completion": """
당신은 파이썬 프로그래밍 교육 전문가입니다.
다음 조건으로 코드 완성 문제를 생성해주세요:

**주제**: {topic}
**난이도**: {difficulty}
**학습 목표**: {learning_objectives}

**요구사항**:
- 실무에서 자주 사용되는 패턴
- 2-4개의 빈칸
- 각 빈칸에 대한 힌트
- 테스트 케이스 3-5개

**JSON 형식으로 응답**:
{{
    "question": "다음 함수를 완성하세요",
    "code_template": "def function_name():\\n    # 빈칸1: ____\\n    # 빈칸2: ____",
    "blanks": ["정답1", "정답2"],
    "blank_hints": ["힌트1", "힌트2"],
    "test_cases": [
        {{"input": "입력값", "output": "예상출력"}},
        {{"input": "입력값2", "output": "예상출력2"}}
    ]
}}
""",

    "debug_code": """
당신은 파이썬 프로그래밍 교육 전문가입니다.
다음 조건으로 디버깅 문제를 생성해주세요:

**주제**: {topic}
**난이도**: {difficulty}
**학습 목표**: {learning_objectives}

**요구사항**:
- 실제 초보자가 자주 하는 실수
- 1-3개의 버그
- 각 버그의 원인과 수정 방법
- 수정된 완전한 코드

**JSON 형식으로 응답**:
{{
    "question": "다음 코드의 오류를 찾아 수정하세요",
    "buggy_code": "잘못된 코드",
    "errors": [
        {{"line": 3, "error": "오류 설명", "fix": "수정 방법"}}
    ],
    "corrected_code": "수정된 완전한 코드",
    "bug_types": ["syntax", "logic"]
}}
""",

    "true_false": """
당신은 파이썬 프로그래밍 교육 전문가입니다.
다음 조건으로 참/거짓 문제를 생성해주세요:

**주제**: {topic}
**난이도**: {difficulty}
**학습 목표**: {learning_objectives}

**요구사항**:
- 명확한 참/거짓 판단 가능
- 일반적인 오해나 혼동 포인트
- 상세한 해설

**JSON 형식으로 응답**:
{{
    "statement": "판단할 문장",
    "correct_answer": true,
    "explanation": "정답 해설",
    "common_misconception": "자주 틀리는 이유"
}}
"""
}
```

---

## 🧪 Phase 2: 테스트 케이스 설계

### 2-1. 단위 테스트 케이스
```python
# test_question_generation.py
import pytest
from app.services.ai_question_generator import AIQuestionGenerator

class TestQuestionGeneration:
    
    @pytest.fixture
    def generator(self):
        return AIQuestionGenerator()
    
    # 객관식 테스트
    async def test_multiple_choice_generation(self, generator):
        result = await generator.generate_question_by_type(
            question_type="multiple_choice",
            topic="변수와 자료형",
            difficulty="easy"
        )
        
        # 필수 필드 존재 확인
        assert "question" in result
        assert "options" in result
        assert len(result["options"]) == 4
        assert "correct_answer" in result
        assert result["correct_answer"] in ["A", "B", "C", "D"]
        assert "explanation" in result
    
    # 주관식 테스트
    async def test_short_answer_generation(self, generator):
        result = await generator.generate_question_by_type(
            question_type="short_answer", 
            topic="리스트와 딕셔너리",
            difficulty="medium"
        )
        
        assert "question" in result
        assert "expected_keywords" in result
        assert len(result["expected_keywords"]) >= 3
        assert "sample_answer" in result
        assert 50 <= len(result["sample_answer"]) <= 300
    
    # 경계값 테스트
    async def test_invalid_question_type(self, generator):
        with pytest.raises(ValueError):
            await generator.generate_question_by_type(
                question_type="invalid_type",
                topic="any_topic", 
                difficulty="easy"
            )
    
    # AI 실패 시 폴백 테스트
    async def test_ai_failure_fallback(self, generator):
        # AI API 실패 시뮬레이션
        with patch('app.services.llm_providers.get_llm_provider') as mock_llm:
            mock_llm.return_value.generate_response.side_effect = Exception("API Error")
            
            result = await generator.generate_question_by_type(
                question_type="multiple_choice",
                topic="변수와 자료형", 
                difficulty="easy"
            )
            
            # 템플릿 폴백 확인
            assert result["ai_generated"] == False
            assert "question" in result
```

### 2-2. 통합 테스트 케이스
```python
# test_question_api.py
async def test_mixed_question_generation_api():
    """5가지 문제 유형 혼합 생성 테스트"""
    
    request_data = {
        "topic": "조건문",
        "difficulty": "medium", 
        "question_types": {
            "multiple_choice": 2,
            "short_answer": 1,
            "code_completion": 1,
            "true_false": 1
        }
    }
    
    response = await client.post(
        "/api/v1/ai-learning/generate-mixed-questions",
        json=request_data,
        headers=auth_headers
    )
    
    assert response.status_code == 200
    data = response.json()
    assert len(data["questions"]) == 5
    
    # 각 문제 유형 확인
    types = [q["type"] for q in data["questions"]]
    assert types.count("multiple_choice") == 2
    assert types.count("short_answer") == 1
    assert types.count("code_completion") == 1
    assert types.count("true_false") == 1
```

---

## ⚙️ Phase 3: 구현 계획

### 3-1. 파일별 구현 순서

#### A) `ai_question_generator.py` 확장
```python
class AIQuestionGenerator:
    
    def __init__(self):
        self.question_types = [
            "multiple_choice", "short_answer", "code_completion", 
            "debug_code", "true_false"
        ]
        self.prompts = QUESTION_GENERATION_PROMPTS
        self.validators = QuestionValidators()
    
    async def generate_question_by_type(
        self, 
        question_type: str,
        topic: str,
        difficulty: str,
        context: Dict = None
    ) -> Dict[str, Any]:
        """문제 유형별 생성 메인 함수"""
        
        # 1. 입력 검증
        if question_type not in self.question_types:
            raise ValueError(f"지원하지 않는 문제 유형: {question_type}")
        
        # 2. AI 프롬프트 생성
        prompt = self._build_prompt(question_type, topic, difficulty, context)
        
        # 3. AI 호출
        try:
            ai_response = await self._call_ai_api(prompt)
            question = self._parse_ai_response(ai_response, question_type)
        except Exception as e:
            # 4. 폴백 시스템
            question = await self._generate_fallback_question(
                question_type, topic, difficulty
            )
        
        # 5. 품질 검증
        validated_question = await self.validators.validate_question(
            question, question_type
        )
        
        return validated_question
    
    async def generate_mixed_question_set(
        self,
        topic: str,
        difficulty: str, 
        question_mix: Dict[str, int]
    ) -> List[Dict[str, Any]]:
        """여러 문제 유형을 한 번에 생성"""
        
        questions = []
        
        for question_type, count in question_mix.items():
            for _ in range(count):
                question = await self.generate_question_by_type(
                    question_type, topic, difficulty
                )
                questions.append(question)
        
        # 문제 순서 셞플
        random.shuffle(questions)
        
        return questions
```

#### B) `scoring_service.py` 확장
```python
async def score_by_question_type(
    self,
    question_type: str,
    question: Dict,
    user_answer: str,
    context: Dict = None
) -> Dict[str, Any]:
    """문제 유형별 채점 로직"""
    
    scoring_methods = {
        "multiple_choice": self._score_multiple_choice,
        "short_answer": self._score_short_answer,
        "code_completion": self._score_code_completion,
        "debug_code": self._score_debug_code,
        "true_false": self._score_true_false
    }
    
    if question_type not in scoring_methods:
        raise ValueError(f"지원하지 않는 문제 유형: {question_type}")
    
    return await scoring_methods[question_type](question, user_answer, context)

async def _score_multiple_choice(self, question, user_answer, context):
    """객관식 채점 - 정답/오답만"""
    correct = (user_answer.upper() == question["correct_answer"])
    score = 1.0 if correct else 0.0
    
    feedback = await self._generate_ai_feedback(
        question, user_answer, score, "multiple_choice"
    )
    
    return {
        "score": score,
        "feedback": feedback,
        "is_correct": correct,
        "explanation": question.get("explanation", "")
    }

async def _score_short_answer(self, question, user_answer, context):
    """주관식 채점 - 키워드 + 의미 분석"""
    
    # 키워드 매칭 점수
    keyword_score = self._calculate_keyword_score(
        user_answer, question["expected_keywords"]
    )
    
    # AI 의미 분석 점수
    semantic_score = await self._calculate_semantic_score(
        user_answer, question["sample_answer"]
    )
    
    # 가중 평균
    criteria = question["scoring_criteria"]
    final_score = (
        keyword_score * criteria["keyword_match"] +
        semantic_score * criteria["semantic_similarity"]
    )
    
    feedback = await self._generate_ai_feedback(
        question, user_answer, final_score, "short_answer"
    )
    
    return {
        "score": final_score,
        "feedback": feedback,
        "keyword_score": keyword_score,
        "semantic_score": semantic_score
    }
```

#### C) API 엔드포인트 확장
```python
# ai_learning.py에 추가
@router.post("/generate-mixed-questions")
async def generate_mixed_question_set(
    request: Dict[str, Any],
    current_user: User = Depends(get_current_user)
):
    """다양한 문제 유형 혼합 생성"""
    
    # 교사/관리자만 접근
    if current_user.role not in ["teacher", "admin"]:
        raise HTTPException(status_code=403, detail="권한이 없습니다")
    
    topic = request.get("topic")
    difficulty = request.get("difficulty", "medium")
    question_mix = request.get("question_types", {
        "multiple_choice": 2,
        "short_answer": 1, 
        "code_completion": 1,
        "true_false": 1
    })
    
    try:
        import app.services.ai_question_generator as aq_module
        ai_question_generator = aq_module.ai_question_generator
        
        questions = await ai_question_generator.generate_mixed_question_set(
            topic=topic,
            difficulty=difficulty,
            question_mix=question_mix
        )
        
        return {
            "success": True,
            "questions": questions,
            "topic": topic,
            "difficulty": difficulty,
            "total_count": len(questions),
            "type_distribution": question_mix
        }
        
    except Exception as e:
        print(f"❌ Mixed Question Generation 실패: {e}")
        raise HTTPException(
            status_code=500, 
            detail=f"문제 생성 실패: {str(e)}"
        )
```

---

## 🚀 Phase 4: 구현 및 테스트 실행 계획

### 4-1. 구현 순서 (2시간)
1. **데이터 구조 정의** (20분)
   - `models/question_types.py` 생성
   - Pydantic 모델 정의

2. **AI 프롬프트 템플릿** (20분)
   - `ai_question_generator.py`에 프롬프트 추가
   - 템플릿 테스트

3. **문제 생성 로직** (40분)
   - `generate_question_by_type` 구현
   - `generate_mixed_question_set` 구현

4. **채점 시스템 확장** (30분)
   - `scoring_service.py` 문제 유형별 채점
   - AI 피드백 개선

5. **API 엔드포인트** (20분)
   - `/generate-mixed-questions` 구현
   - 기존 API 확장

6. **통합 테스트** (10분)
   - PowerShell로 각 문제 유형 테스트
   - 혼합 문제 세트 생성 테스트

### 4-2. 테스트 명령어
```powershell
# 혼합 문제 생성 테스트
$mixedRequest = @{
    topic = "조건문"
    difficulty = "medium"
    question_types = @{
        multiple_choice = 2
        short_answer = 1
        code_completion = 1
        true_false = 1
    }
} | ConvertTo-Json

$mixedResult = Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/v1/ai-learning/generate-mixed-questions" -Method POST -Body $mixedRequest -ContentType "application/json" -WebSession $session

Write-Host "=== 혼합 문제 생성 결과 ==="
$mixedResult | ConvertTo-Json -Depth 10
```

---

## ✅ 성공 기준

### 기능적 요구사항
- [ ] 5가지 문제 유형 모두 정상 생성
- [ ] 각 문제 유형별 적절한 채점 로직
- [ ] AI 실패 시 템플릿 폴백 작동
- [ ] 문제 품질 검증 시스템

### 성능 요구사항  
- [ ] 문제 생성 시간: 15초 이내
- [ ] API 응답률: 95% 이상
- [ ] 메모리 사용량: 현재 대비 +30% 이내

### 품질 요구사항
- [ ] 생성된 문제의 교육적 적절성
- [ ] JSON 파싱 성공률 90% 이상
- [ ] 문제 중복률 5% 이하

---

## 🎯 최종 목표

**"매일매일 진행되는 수업 진도에 맞춰 AI가 다양한 유형의 문제를 자동 생성하고, 각 유형에 맞는 정확한 채점과 피드백을 제공하는 시스템"**

이 계획대로 구현하면 오늘 안에 완전한 AI 문제 생성 자동화 시스템이 완성됩니다! 🚀

**준비되면 바로 구현 시작하겠습니다!**
