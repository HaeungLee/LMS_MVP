# PostgreSQL 데이터베이스 설정 (Docker 연동)
DATABASE_URL=postgresql://lms_user:1234@localhost:15432/lms_mvp_db

# 개발 환경 설정
ENVIRONMENT=development
DEBUG=True

# API 설정
API_HOST=
API_PORT=

# PostgreSQL 직접 연결 설정
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# OpenRouter AI 설정
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=qwen/qwen3-coder:free

# LLM 설정
LLM_PROVIDER=openrouter
LLM_ENABLED=true
LLM_TIMEOUT_MS=15000
LLM_MAX_RETRIES=2
LLM_CACHE_TTL_SECONDS=600
LLM_MAX_RPM=60