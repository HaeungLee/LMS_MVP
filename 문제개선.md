# 📝 LMS MVP 문제 출제 시스템 개선 계획

## 🎯 목표
교사가 직접 문제를 출제할 수 있는 관리자 시스템을 구축하고, LLM 기반 주관식 채점 시스템을 도입하여 문제 데이터베이스를 확장한다.

---

## 📋 현재 상황 분석

### ✅ 기존 시스템
- **문제 형태**: 객관식 위주 (Python 18문제)
- **데이터 저장**: JSON 파일 기반
- **사용자 구분**: 없음 (모든 사용자 동일)
- **문제 관리**: 개발자가 직접 JSON 수정

### 🚨 문제점
1. **문제 추가의 어려움**: 개발자만 문제 추가 가능
2. **사용자 권한 미구분**: 교사/학생 구별 없음
3. **주관식 문제 부재**: 코딩 문제의 다양성 부족
4. **확장성 제한**: JSON 기반으로 대량 데이터 처리 한계

---

## 🛠 단계별 개발 계획

### 📍 **1단계: 기반 인프라 구축 (1주차)**

#### 1.1 사용자 인증 시스템 구축
```
우선순위: ⭐⭐⭐ (필수)
예상 시간: 3-4일
```

**백엔드 작업:**
- [ ] JWT 기반 인증 시스템 구현
- [ ] 사용자 등록/로그인 API 개발
- [ ] 권한 미들웨어 구현 (admin/teacher/student)
- [ ] 비밀번호 암호화 (bcrypt)

**프론트엔드 작업:**
- [ ] 로그인/회원가입 페이지 구현
- [ ] 사용자 상태 관리 (Zustand)
- [ ] 권한별 라우팅 가드
- [ ] 인증 토큰 관리

**데이터베이스 스키마:**
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL DEFAULT 'student', -- student, teacher, admin
    full_name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT true
);
```

#### 1.2 PostgreSQL 마이그레이션
```
우선순위: ⭐⭐⭐ (필수)
예상 시간: 2-3일
```

**문제 테이블 확장:**
```sql
CREATE TABLE questions (
    id SERIAL PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    subject VARCHAR(50) NOT NULL, -- python, javascript, html_css, sql, algorithm
    topic VARCHAR(100) NOT NULL,
    question_type VARCHAR(20) NOT NULL, -- multiple_choice, coding, essay
    difficulty VARCHAR(10) NOT NULL, -- easy, medium, hard
    
    -- 문제 내용
    question_text TEXT NOT NULL,
    code_snippet TEXT,
    options JSONB, -- 객관식 선택지
    
    -- 정답 및 채점 기준
    correct_answer TEXT,
    answer_type VARCHAR(20) NOT NULL, -- exact, code_execution, llm_grading
    grading_rubric TEXT, -- LLM 채점용 루브릭
    
    -- 메타데이터
    created_by INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT true,
    
    -- 통계
    total_attempts INTEGER DEFAULT 0,
    correct_attempts INTEGER DEFAULT 0
);
```

**기타 필요 테이블:**
```sql
-- 사용자 문제 풀이 기록
CREATE TABLE user_submissions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    question_id INTEGER REFERENCES questions(id),
    user_answer TEXT NOT NULL,
    is_correct BOOLEAN,
    score DECIMAL(5,2), -- 0-100점
    ai_feedback TEXT,
    submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 퀴즈 세션 관리
CREATE TABLE quiz_sessions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    subject VARCHAR(50) NOT NULL,
    total_questions INTEGER NOT NULL,
    correct_answers INTEGER DEFAULT 0,
    total_score DECIMAL(5,2) DEFAULT 0,
    started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    time_spent INTEGER -- 초 단위
);
```

---

### 📍 **2단계: 관리자 문제 출제 시스템 (2주차)**

#### 2.1 문제 출제 관리자 페이지
```
우선순위: ⭐⭐⭐ (필수)
예상 시간: 4-5일
```

**기능 요구사항:**
- [ ] 게시판 형식 문제 목록 (페이지네이션)
- [ ] 문제 CRUD 인터페이스
- [ ] 실시간 미리보기 기능
- [ ] 코드 에디터 (Monaco Editor 통합)
- [ ] 이미지 업로드 지원
- [ ] 문제 템플릿 제공

**UI 컴포넌트 구조:**
```
src/pages/admin/
├── AdminDashboard.jsx      # 관리자 메인 대시보드
├── QuestionList.jsx        # 문제 목록 (게시판)
├── QuestionForm.jsx        # 문제 출제 폼
├── QuestionPreview.jsx     # 문제 미리보기
└── QuestionStats.jsx       # 문제별 통계

src/components/admin/
├── CodeEditor.jsx          # Monaco 에디터 래퍼
├── QuestionTypeSelector.jsx # 문제 유형 선택
├── DifficultySelector.jsx  # 난이도 선택
├── SubjectSelector.jsx     # 과목 선택
└── RubricEditor.jsx        # 채점 기준 에디터
```

#### 2.2 문제 유형별 템플릿 시스템
```
우선순위: ⭐⭐ (중요)
예상 시간: 2-3일
```

**템플릿 구조:**
```javascript
const questionTemplates = {
  python: {
    multiple_choice: {
      template: "다음 Python 코드의 출력 결과는?",
      example_code: "print('Hello, World!')",
      options: ["A) Hello, World!", "B) 오류", "C) None", "D) hello, world!"]
    },
    coding: {
      template: "다음 요구사항을 만족하는 함수를 작성하세요.",
      example_prompt: "리스트에서 중복을 제거하는 함수를 작성하세요.",
      starter_code: "def remove_duplicates(lst):\n    # 여기에 코드를 작성하세요\n    pass"
    },
    essay: {
      template: "다음 개념에 대해 설명하세요.",
      example_prompt: "Python의 리스트 컴프리헨션의 장점과 사용법을 설명하세요."
    }
  },
  javascript: {
    // 유사한 구조...
  }
  // 다른 과목들...
};
```

---

### 📍 **3단계: LLM 기반 채점 시스템 (3주차)**

#### 3.1 AI 채점 엔진 구현
```
우선순위: ⭐⭐⭐ (필수)
예상 시간: 3-4일
```

**채점 방식별 구현:**

1. **객관식 문제**: 정확 일치 비교
2. **코딩 문제**: 코드 실행 + 결과 비교
3. **주관식 문제**: LLM 기반 채점

**LLM 채점 프롬프트 시스템:**
```python
class AIGradingSystem:
    def __init__(self):
        self.grading_prompt_template = """
다음은 프로그래밍 교육 문제와 학생의 답안입니다.

문제: {question_text}
채점 기준: {grading_rubric}
학생 답안: {student_answer}

다음 기준으로 평가해주세요:
1. 정확성 (40점): 답안이 문제 요구사항을 정확히 충족하는가?
2. 완성도 (30점): 답안이 완전하고 논리적인가?
3. 이해도 (20점): 개념을 올바르게 이해하고 있는가?
4. 표현력 (10점): 명확하고 이해하기 쉽게 작성했는가?

응답 형식:
{{
  "score": 85,
  "detailed_scores": {{
    "accuracy": 35,
    "completeness": 25,
    "understanding": 18,
    "clarity": 7
  }},
  "feedback": "상세한 피드백 내용...",
  "suggestions": ["개선 제안 1", "개선 제안 2"]
}}
"""
```

#### 3.2 코드 실행 엔진
```
우선순위: ⭐⭐ (중요)
예상 시간: 2-3일
```

**안전한 코드 실행 환경:**
```python
import subprocess
import tempfile
import os
from typing import Dict, Any

class CodeExecutor:
    def __init__(self):
        self.supported_languages = {
            'python': self._execute_python,
            'javascript': self._execute_javascript,
            'sql': self._execute_sql
        }
    
    def execute_code(self, code: str, language: str, test_cases: list) -> Dict[str, Any]:
        """안전한 환경에서 코드 실행 및 테스트"""
        if language not in self.supported_languages:
            raise ValueError(f"Unsupported language: {language}")
        
        return self.supported_languages[language](code, test_cases)
    
    def _execute_python(self, code: str, test_cases: list) -> Dict[str, Any]:
        # Docker 컨테이너나 샌드박스 환경에서 실행
        # 시간 제한, 메모리 제한 적용
        pass
```

---

### 📍 **4단계: 과목별 문제 카테고리화 및 시각화 (4주차)**

#### 4.1 과목별 카테고리 체계
```
우선순위: ⭐⭐ (중요)
예상 시간: 2-3일
```

**과목별 세부 카테고리:**

```javascript
const subjectCategories = {
  python: {
    name: "Python 프로그래밍",
    color: "#3776ab",
    icon: "🐍",
    categories: [
      {
        id: "python_basics",
        name: "Python 기초",
        subcategories: ["변수와 데이터 타입", "연산자", "입출력", "조건문", "반복문"]
      },
      {
        id: "python_datastructures",
        name: "자료구조",
        subcategories: ["리스트", "튜플", "딕셔너리", "집합", "문자열"]
      },
      {
        id: "python_functions",
        name: "함수와 모듈",
        subcategories: ["함수 정의", "매개변수", "람다 함수", "모듈과 패키지"]
      },
      {
        id: "python_oop",
        name: "객체지향 프로그래밍",
        subcategories: ["클래스와 객체", "상속", "캡슐화", "다형성"]
      }
    ]
  },

  javascript: {
    name: "JavaScript",
    color: "#f7df1e",
    icon: "⚡",
    categories: [
      {
        id: "js_basics",
        name: "JavaScript 기초",
        subcategories: ["변수 선언", "데이터 타입", "연산자", "조건문", "반복문"]
      },
      {
        id: "js_functions",
        name: "함수",
        subcategories: ["함수 선언", "화살표 함수", "콜백 함수", "클로저"]
      },
      {
        id: "js_objects",
        name: "객체와 배열",
        subcategories: ["객체 리터럴", "배열 메서드", "구조분해할당", "스프레드 연산자"]
      },
      {
        id: "js_async",
        name: "비동기 프로그래밍",
        subcategories: ["Promise", "async/await", "이벤트 루프", "AJAX"]
      },
      {
        id: "js_dom",
        name: "DOM 조작",
        subcategories: ["요소 선택", "이벤트 처리", "스타일 변경", "동적 콘텐츠"]
      }
    ]
  },

  html_css: {
    name: "HTML/CSS",
    color: "#e34c26",
    icon: "🎨",
    categories: [
      {
        id: "html_structure",
        name: "HTML 구조",
        subcategories: ["기본 태그", "시맨틱 HTML", "폼 요소", "멀티미디어"]
      },
      {
        id: "css_styling",
        name: "CSS 스타일링",
        subcategories: ["선택자", "박스 모델", "색상과 글꼴", "배경과 테두리"]
      },
      {
        id: "css_layout",
        name: "CSS 레이아웃",
        subcategories: ["Flexbox", "Grid", "Position", "Float"]
      },
      {
        id: "css_responsive",
        name: "반응형 웹",
        subcategories: ["미디어 쿼리", "Viewport", "유연한 이미지", "모바일 퍼스트"]
      }
    ]
  },

  sql: {
    name: "SQL 데이터베이스",
    color: "#00758f",
    icon: "🗄️",
    categories: [
      {
        id: "sql_basics",
        name: "SQL 기초",
        subcategories: ["SELECT 문", "WHERE 조건", "ORDER BY", "GROUP BY"]
      },
      {
        id: "sql_joins",
        name: "테이블 조인",
        subcategories: ["INNER JOIN", "LEFT JOIN", "RIGHT JOIN", "FULL JOIN"]
      },
      {
        id: "sql_functions",
        name: "SQL 함수",
        subcategories: ["집계 함수", "문자열 함수", "날짜 함수", "수학 함수"]
      },
      {
        id: "sql_advanced",
        name: "고급 SQL",
        subcategories: ["서브쿼리", "윈도우 함수", "CTE", "저장 프로시저"]
      }
    ]
  },

  algorithm: {
    name: "알고리즘",
    color: "#8e44ad",
    icon: "🧮",
    categories: [
      {
        id: "algo_search",
        name: "탐색 알고리즘",
        subcategories: ["선형 탐색", "이진 탐색", "BFS", "DFS"]
      },
      {
        id: "algo_sort",
        name: "정렬 알고리즘",
        subcategories: ["버블 정렬", "선택 정렬", "삽입 정렬", "퀵 정렬", "병합 정렬"]
      },
      {
        id: "algo_dynamic",
        name: "동적 프로그래밍",
        subcategories: ["메모이제이션", "타뷸레이션", "최적 부분 구조", "LCS", "LIS"]
      },
      {
        id: "algo_graph",
        name: "그래프 알고리즘",
        subcategories: ["최단 경로", "최소 신장 트리", "위상 정렬", "강연결 성분"]
      }
    ]
  },

  datastructure: {
    name: "자료구조",
    color: "#2c3e50",
    icon: "📊",
    categories: [
      {
        id: "ds_linear",
        name: "선형 자료구조",
        subcategories: ["배열", "연결 리스트", "스택", "큐", "덱"]
      },
      {
        id: "ds_tree",
        name: "트리 구조",
        subcategories: ["이진 트리", "BST", "AVL 트리", "힙", "트라이"]
      },
      {
        id: "ds_hash",
        name: "해시 구조",
        subcategories: ["해시 테이블", "해시 함수", "충돌 처리", "블룸 필터"]
      },
      {
        id: "ds_graph",
        name: "그래프 구조",
        subcategories: ["인접 리스트", "인접 행렬", "가중 그래프", "방향 그래프"]
      }
    ]
  }
};
```

#### 4.2 시각화 대시보드 설계
```
우선순위: ⭐⭐ (중요)
예상 시간: 3-4일
```

**시각화 컴포넌트:**

1. **과목별 문제 분포 차트**
```javascript
// 도넛 차트로 과목별 문제 수 시각화
const SubjectDistributionChart = () => {
  return (
    <div className="chart-container">
      <DonutChart
        data={subjectData}
        colors={subjects.map(s => s.color)}
        centerText="총 문제 수"
      />
    </div>
  );
};
```

2. **난이도별 분포 히트맵**
```javascript
// 과목 x 난이도 2D 히트맵
const DifficultyHeatmap = () => {
  return (
    <div className="heatmap-container">
      {subjects.map(subject => (
        <div key={subject.id} className="heatmap-row">
          <span className="subject-label">{subject.name}</span>
          {['easy', 'medium', 'hard'].map(difficulty => (
            <div 
              key={difficulty}
              className="heatmap-cell"
              style={{
                backgroundColor: getHeatmapColor(
                  getQuestionCount(subject.id, difficulty)
                )
              }}
            >
              {getQuestionCount(subject.id, difficulty)}
            </div>
          ))}
        </div>
      ))}
    </div>
  );
};
```

3. **카테고리별 상세 트리맵**
```javascript
// 각 과목 내 세부 카테고리 시각화
const CategoryTreemap = ({ subject }) => {
  return (
    <div className="treemap-container">
      <TreemapChart
        data={subject.categories}
        getValue={category => category.questionCount}
        getColor={category => category.completionRate}
      />
    </div>
  );
};
```

4. **학습 진도 레이더 차트**
```javascript
// 사용자별 과목 진도 시각화
const LearningProgressRadar = ({ userId }) => {
  return (
    <RadarChart
      data={userProgress}
      axes={subjects.map(s => s.name)}
      maxValue={100}
    />
  );
};
```

---

## 🎨 UI/UX 설계

### 관리자 문제 출제 인터페이스
```
┌─────────────────────────────────────────────────────────────┐
│ 🔧 문제 관리 시스템                                          │
├─────────────────────────────────────────────────────────────┤
│ [새 문제 출제] [문제 목록] [통계 보기] [설정]                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─ 문제 기본 정보 ──────────────────────────────────────┐    │
│ │ 제목: [                                            ] │    │
│ │ 과목: [Python ▼] 카테고리: [자료구조 ▼]              │    │
│ │ 난이도: ○Easy ○Medium ○Hard                          │    │
│ │ 유형: ○객관식 ○코딩 ○주관식                           │    │
│ └─────────────────────────────────────────────────────┘    │
│                                                             │
│ ┌─ 문제 내용 ──────────────────────────────────────────┐    │
│ │ 문제 설명:                                           │    │
│ │ ┌─────────────────────────────────────────────────┐  │    │
│ │ │ 다음 Python 코드의 출력 결과를 선택하세요.        │  │    │
│ │ │                                                 │  │    │
│ │ │ ```python                                       │  │    │
│ │ │ numbers = [1, 2, 3, 4, 5]                      │  │    │
│ │ │ result = [x * 2 for x in numbers if x % 2 == 0] │  │    │
│ │ │ print(result)                                   │  │    │
│ │ │ ```                                             │  │    │
│ │ └─────────────────────────────────────────────────┘  │    │
│ └─────────────────────────────────────────────────────┘    │
│                                                             │
│ ┌─ 선택지/정답 ──────────────────────────────────────────┐   │
│ │ A) [1, 2, 3, 4, 5]                                   │   │
│ │ B) [2, 4] ✓                                          │   │
│ │ C) [4, 8]                                            │   │
│ │ D) []                                                │   │
│ └─────────────────────────────────────────────────────┘   │
│                                                             │
│ ┌─ 채점 기준 (주관식용) ──────────────────────────────────┐   │
│ │ 이 문제는 Python 리스트 컴프리헨션의 이해를 평가합니다.  │   │
│ │ - 조건부 리스트 컴프리헨션 문법 이해 (30점)              │   │
│ │ - 짝수 판별 로직 이해 (30점)                           │   │
│ │ - 결과 예측 능력 (40점)                               │   │
│ └─────────────────────────────────────────────────────┘   │
│                                                             │
│ [미리보기] [임시저장] [문제 등록]                             │
└─────────────────────────────────────────────────────────────┘
```

### 과목별 시각화 대시보드
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 문제 현황 대시보드                                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─ 과목별 분포 ─┐  ┌─ 난이도 분포 ─────────────────────┐     │
│ │      🐍       │  │      Easy   Medium   Hard        │     │
│ │   Python      │  │ Python   ████   ██████   ███     │     │
│ │    (45%)      │  │ JS       ██     ████     ██      │     │
│ │               │  │ HTML     ███    ███      █       │     │
│ │   ⚡ JS (25%) │  │ SQL      ██     ██       ████    │     │
│ │   🎨 CSS(15%) │  │ Algo     █      ███      █████   │     │
│ │   🗄️ SQL(15%) │  └─────────────────────────────────────────┘     │
│ └───────────────┘                                           │
│                                                             │
│ ┌─ 최근 출제된 문제 ───────────────────────────────────────┐  │
│ │ • Python 딕셔너리 활용 (Medium) - 2025.08.07            │  │
│ │ • JavaScript 비동기 처리 (Hard) - 2025.08.06            │  │
│ │ • SQL JOIN 활용 (Medium) - 2025.08.05                   │  │
│ │ • 이진 탐색 구현 (Hard) - 2025.08.04                    │  │
│ └─────────────────────────────────────────────────────────┘  │
│                                                             │
│ ┌─ 문제 품질 통계 ─────────────────────────────────────────┐  │
│ │ 평균 정답률: 73.2% | 평균 풀이 시간: 4분 32초            │  │
│ │ 가장 어려운 문제: "동적 프로그래밍 최적화" (정답률 23%)    │  │
│ │ 가장 쉬운 문제: "Python 변수 선언" (정답률 98%)           │  │
│ └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 구현 우선순위

### Phase 1: 필수 기능 (2주)
1. **사용자 인증 시스템** - 교사/학생 구분
2. **PostgreSQL 마이그레이션** - 확장 가능한 DB 구조
3. **기본 문제 출제 인터페이스** - CRUD 기능

### Phase 2: 고급 기능 (2주)
1. **LLM 채점 시스템** - 주관식 자동 채점
2. **코드 실행 엔진** - 코딩 문제 자동 검증
3. **과목별 카테고리 시각화** - 대시보드 구현

### Phase 3: 최적화 (1주)
1. **성능 최적화** - 대량 문제 처리
2. **사용자 경험 개선** - UI/UX 고도화
3. **보안 강화** - 권한 관리 세분화

---

---

## 🎯 **Python 체계적 학습 진도 시스템**

### 📚 Python 학습 단계별 구조
```
우선순위: ⭐⭐⭐ (핵심 기능)
예상 시간: 1주
```

**학습 진도 체계:**
```javascript
const pythonLearningPath = {
  id: "python_fundamentals",
  name: "Python 기초 완주",
  totalSteps: 10,
  description: "Python 프로그래밍의 핵심 개념을 단계별로 학습합니다.",
  
  steps: [
    {
      id: "python_basics",
      name: "Python 기초",
      order: 1,
      icon: "🌱",
      color: "#e8f5e8",
      description: "파이썬 프로그래밍 시작하기",
      topics: ["파이썬 소개", "개발환경 설정", "기본 문법", "주석", "들여쓰기"],
      estimatedTime: "30분",
      questionCount: 5
    },
    {
      id: "operators",
      name: "연산자",
      order: 2,
      icon: "➕",
      color: "#f0f8ff",
      description: "다양한 연산자 사용법 익히기",
      topics: ["산술연산자", "비교연산자", "논리연산자", "할당연산자", "멤버십연산자"],
      estimatedTime: "45분",
      questionCount: 6
    },
    {
      id: "variables",
      name: "변수",
      order: 3,
      icon: "📦",
      color: "#fff5ee",
      description: "변수 선언과 사용법 학습",
      topics: ["변수 선언", "변수 명명 규칙", "변수 스코프", "전역변수", "지역변수"],
      estimatedTime: "40분",
      questionCount: 5
    },
    {
      id: "data_types",
      name: "자료형",
      order: 4,
      icon: "🔤",
      color: "#f5f0ff",
      description: "파이썬의 기본 자료형 이해",
      topics: ["정수형", "실수형", "문자열", "불린형", "타입변환"],
      estimatedTime: "50분",
      questionCount: 7
    },
    {
      id: "tuples",
      name: "튜플",
      order: 5,
      icon: "📝",
      color: "#f0fff0",
      description: "불변 순서형 자료구조 튜플",
      topics: ["튜플 생성", "튜플 인덱싱", "튜플 언패킹", "튜플 메서드", "네임드 튜플"],
      estimatedTime: "35분",
      questionCount: 4
    },
    {
      id: "lists",
      name: "리스트",
      order: 6,
      icon: "📋",
      color: "#fff0f5",
      description: "가변 순서형 자료구조 리스트",
      topics: ["리스트 생성", "리스트 인덱싱", "리스트 슬라이싱", "리스트 메서드", "리스트 컴프리헨션"],
      estimatedTime: "60분",
      questionCount: 8
    },
    {
      id: "dictionaries",
      name: "딕셔너리",
      order: 7,
      icon: "🗂️",
      color: "#f0ffff",
      description: "키-값 쌍의 자료구조 딕셔너리",
      topics: ["딕셔너리 생성", "키-값 접근", "딕셔너리 메서드", "딕셔너리 컴프리헨션", "중첩 딕셔너리"],
      estimatedTime: "55분",
      questionCount: 7
    },
    {
      id: "conditionals",
      name: "조건문",
      order: 8,
      icon: "🔀",
      color: "#fffacd",
      description: "조건에 따른 프로그램 분기",
      topics: ["if문", "elif문", "else문", "중첩 조건문", "삼항 연산자"],
      estimatedTime: "45분",
      questionCount: 6
    },
    {
      id: "loops",
      name: "반복문",
      order: 9,
      icon: "�",
      color: "#fdf5e6",
      description: "반복 실행을 위한 반복문",
      topics: ["for문", "while문", "break와 continue", "중첩 반복문", "enumerate와 zip"],
      estimatedTime: "65분",
      questionCount: 8
    },
    {
      id: "functions",
      name: "함수",
      order: 10,
      icon: "⚙️",
      color: "#f5fffa",
      description: "코드 재사용을 위한 함수",
      topics: ["함수 정의", "매개변수", "반환값", "람다 함수", "데코레이터 기초"],
      estimatedTime: "70분",
      questionCount: 9
    },
    {
      id: "classes",
      name: "클래스",
      order: 11,
      icon: "🏗️",
      color: "#f0f8ff",
      description: "객체지향 프로그래밍의 기초",
      topics: ["클래스 정의", "인스턴스 생성", "메서드", "상속", "특수 메서드"],
      estimatedTime: "80분",
      questionCount: 10
    }
  ]
};
```

### 🎮 학습 진도 관리 시스템

#### 사용자 진도 데이터 구조
```javascript
const userProgress = {
  userId: 123,
  pythonPath: {
    currentStep: 3, // 현재 학습 중인 단계
    completedSteps: [1, 2], // 완료한 단계들
    stepProgress: {
      1: { completed: true, score: 85, completedAt: "2025-08-01" },
      2: { completed: true, score: 92, completedAt: "2025-08-03" },
      3: { completed: false, score: null, attemptCount: 2, lastAttempt: "2025-08-07" }
    },
    overallProgress: 27.3, // (2 완료 + 0.5 진행중) / 11 * 100
    totalScore: 88.5, // 완료한 단계들의 평균 점수
    estimatedCompletion: "2025-08-25", // 예상 완료일
    weeklyGoal: 2, // 주당 목표 단계 수
    streakDays: 5 // 연속 학습일
  }
};
```

#### 학습 단계별 퀴즈 생성 로직
```python
class PythonPathQuizGenerator:
    def __init__(self):
        self.step_question_distribution = {
            "python_basics": {"easy": 3, "medium": 2, "hard": 0},
            "operators": {"easy": 3, "medium": 2, "hard": 1},
            "variables": {"easy": 3, "medium": 2, "hard": 0},
            "data_types": {"easy": 4, "medium": 2, "hard": 1},
            "tuples": {"easy": 2, "medium": 2, "hard": 0},
            "lists": {"easy": 4, "medium": 3, "hard": 1},
            "dictionaries": {"easy": 3, "medium": 3, "hard": 1},
            "conditionals": {"easy": 3, "medium": 2, "hard": 1},
            "loops": {"easy": 4, "medium": 3, "hard": 1},
            "functions": {"easy": 4, "medium": 4, "hard": 1},
            "classes": {"easy": 4, "medium": 4, "hard": 2}
        }
    
    def generate_step_quiz(self, step_id: str, user_id: int) -> List[Dict]:
        """특정 학습 단계의 퀴즈를 생성합니다."""
        distribution = self.step_question_distribution.get(step_id)
        
        questions = []
        for difficulty, count in distribution.items():
            step_questions = self.get_questions_by_step_and_difficulty(step_id, difficulty)
            selected = random.sample(step_questions, min(count, len(step_questions)))
            questions.extend(selected)
        
        # 문제 순서 셔플
        random.shuffle(questions)
        
        return questions
    
    def get_next_recommended_step(self, user_id: int) -> Dict:
        """사용자에게 다음 추천 학습 단계를 반환합니다."""
        user_progress = self.get_user_progress(user_id)
        
        # 미완료 단계 중 가장 낮은 order 찾기
        completed_steps = user_progress.get('completedSteps', [])
        for step in pythonLearningPath['steps']:
            if step['order'] not in completed_steps:
                return {
                    "recommendedStep": step,
                    "reason": self._get_recommendation_reason(step, user_progress),
                    "estimatedTime": step['estimatedTime'],
                    "prerequisitesMet": self._check_prerequisites(step, completed_steps)
                }
        
        return {"message": "축하합니다! Python 기초 과정을 모두 완료했습니다!"}
```

---

## 🎨 **시각화 디자인 시스템**

### 1. 학습 경로 로드맵 시각화
```
┌─────────────────────────────────────────────────────────────┐
│ 🐍 Python 기초 완주 로드맵                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 🌱────➕────📦────🔤────📝────📋────🗂️────🔀────🔄────⚙️────🏗️ │
│ 기초   연산자  변수   자료형  튜플   리스트  딕셔너리 조건문  반복문  함수   클래스│
│ ✅    ✅    🔄    ⏳    ⏳    ⏳    ⏳    ⏳    ⏳    ⏳    ⏳ │
│ 85점   92점   진행중                                          │
│                                                             │
│ 📊 전체 진도: ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 27.3%    │
│ 🎯 이번 주 목표: 변수, 자료형 완료 (2/2)                      │
│ 🔥 연속 학습: 5일째                                          │
└─────────────────────────────────────────────────────────────┘
```

### 2. 단계별 상세 진도 카드
```javascript
const StepProgressCard = ({ step, userProgress }) => {
  const stepData = userProgress.stepProgress[step.order];
  const isCompleted = stepData?.completed;
  const isInProgress = userProgress.currentStep === step.order;
  const isLocked = step.order > userProgress.currentStep + 1;

  return (
    <div className={`step-card ${isCompleted ? 'completed' : ''} ${isInProgress ? 'current' : ''} ${isLocked ? 'locked' : ''}`}>
      <div className="step-header">
        <span className="step-icon" style={{fontSize: '2rem'}}>{step.icon}</span>
        <div className="step-info">
          <h3>{step.name}</h3>
          <p className="step-description">{step.description}</p>
        </div>
        <div className="step-status">
          {isCompleted && <span className="status-badge completed">✅ 완료</span>}
          {isInProgress && <span className="status-badge current">🔄 학습중</span>}
          {isLocked && <span className="status-badge locked">🔒 잠김</span>}
        </div>
      </div>
      
      <div className="step-content">
        <div className="step-topics">
          <h4>학습 주제:</h4>
          <div className="topic-tags">
            {step.topics.map(topic => (
              <span key={topic} className="topic-tag">{topic}</span>
            ))}
          </div>
        </div>
        
        <div className="step-metrics">
          <div className="metric">
            <span className="metric-label">문제 수</span>
            <span className="metric-value">{step.questionCount}문제</span>
          </div>
          <div className="metric">
            <span className="metric-label">예상 시간</span>
            <span className="metric-value">{step.estimatedTime}</span>
          </div>
          {stepData?.score && (
            <div className="metric">
              <span className="metric-label">점수</span>
              <span className="metric-value">{stepData.score}점</span>
            </div>
          )}
        </div>
        
        {!isLocked && (
          <button className={`start-quiz-btn ${isCompleted ? 'retry' : 'start'}`}>
            {isCompleted ? '다시 풀기' : isInProgress ? '계속하기' : '시작하기'}
          </button>
        )}
      </div>
    </div>
  );
};
```

### 3. 원형 진도 차트 (Circular Progress)
```javascript
const CircularProgress = ({ currentStep, totalSteps, completedSteps }) => {
  const progressPercentage = (completedSteps.length / totalSteps) * 100;
  const circumference = 2 * Math.PI * 45; // 반지름 45px
  const strokeDasharray = circumference;
  const strokeDashoffset = circumference - (progressPercentage / 100) * circumference;

  return (
    <div className="circular-progress-container">
      <svg width="120" height="120" className="circular-progress">
        {/* 배경 원 */}
        <circle
          cx="60"
          cy="60"
          r="45"
          stroke="#e6e6e6"
          strokeWidth="8"
          fill="transparent"
        />
        {/* 진도 원 */}
        <circle
          cx="60"
          cy="60"
          r="45"
          stroke="#4CAF50"
          strokeWidth="8"
          fill="transparent"
          strokeDasharray={strokeDasharray}
          strokeDashoffset={strokeDashoffset}
          strokeLinecap="round"
          transform="rotate(-90 60 60)"
          className="progress-circle"
        />
      </svg>
      <div className="progress-text">
        <div className="progress-percentage">{Math.round(progressPercentage)}%</div>
        <div className="progress-details">{completedSteps.length}/{totalSteps}</div>
      </div>
    </div>
  );
};
```

### 4. 학습 통계 대시보드
```javascript
const LearningStatsDashboard = ({ userProgress }) => {
  return (
    <div className="stats-dashboard">
      <div className="stats-grid">
        <div className="stat-card">
          <div className="stat-icon">📚</div>
          <div className="stat-content">
            <div className="stat-value">{userProgress.completedSteps.length}</div>
            <div className="stat-label">완료한 단계</div>
          </div>
        </div>
        
        <div className="stat-card">
          <div className="stat-icon">⭐</div>
          <div className="stat-content">
            <div className="stat-value">{userProgress.totalScore.toFixed(1)}</div>
            <div className="stat-label">평균 점수</div>
          </div>
        </div>
        
        <div className="stat-card">
          <div className="stat-icon">🔥</div>
          <div className="stat-content">
            <div className="stat-value">{userProgress.streakDays}</div>
            <div className="stat-label">연속 학습일</div>
          </div>
        </div>
        
        <div className="stat-card">
          <div className="stat-icon">📅</div>
          <div className="stat-content">
            <div className="stat-value">{userProgress.estimatedCompletion}</div>
            <div className="stat-label">예상 완료일</div>
          </div>
        </div>
      </div>
    </div>
  );
};
```

### 5. 취약점 분석 레이더 차트
```javascript
const WeaknessAnalysisRadar = ({ userStepScores }) => {
  const radarData = pythonLearningPath.steps.map(step => ({
    subject: step.name,
    score: userStepScores[step.id] || 0,
    fullMark: 100
  }));

  return (
    <div className="weakness-analysis">
      <h3>🎯 학습 영역별 점수</h3>
      <RadarChart width={400} height={300} data={radarData}>
        <PolarGrid />
        <PolarAngleAxis dataKey="subject" />
        <PolarRadiusAxis angle={30} domain={[0, 100]} />
        <Radar
          name="점수"
          dataKey="score"
          stroke="#8884d8"
          fill="#8884d8"
          fillOpacity={0.6}
        />
      </RadarChart>
      
      <div className="weakness-insights">
        <h4>💡 학습 제안</h4>
        <ul>
          <li>리스트 컴프리헨션 문제를 더 연습해보세요</li>
          <li>딕셔너리 메서드 활용법을 복습하세요</li>
          <li>함수의 매개변수 전달 방식을 이해하세요</li>
        </ul>
      </div>
    </div>
  );
};
```

### 6. 학습 일정 히트맵
```javascript
const LearningHeatmap = ({ learningHistory }) => {
  // 최근 12주간의 학습 활동을 히트맵으로 표시
  const weeks = generateWeeklyData(learningHistory);
  
  return (
    <div className="learning-heatmap">
      <h3>📈 학습 활동 히트맵</h3>
      <div className="heatmap-grid">
        {weeks.map((week, weekIndex) => (
          <div key={weekIndex} className="week-column">
            {week.days.map((day, dayIndex) => (
              <div
                key={dayIndex}
                className={`day-cell intensity-${day.intensity}`}
                title={`${day.date}: ${day.questionsCompleted}문제 완료`}
              />
            ))}
          </div>
        ))}
      </div>
      <div className="heatmap-legend">
        <span>적음</span>
        <div className="legend-scale">
          <div className="intensity-0"></div>
          <div className="intensity-1"></div>
          <div className="intensity-2"></div>
          <div className="intensity-3"></div>
          <div className="intensity-4"></div>
        </div>
        <span>많음</span>
      </div>
    </div>
  );
};
```

---

## 📊 **구현 우선순위 및 기술 스택**

### Phase 1: 학습 경로 기본 구조 (3일)
```javascript
// 1. 학습 단계 데이터 모델링
// 2. 사용자 진도 추적 시스템
// 3. 단계별 퀴즈 생성 로직
// 4. 기본 진도 시각화 (원형 차트, 카드뷰)
```

### Phase 2: 고급 시각화 (3일)
```javascript
// 1. 학습 경로 로드맵 인터랙티브 UI
// 2. 레이더 차트 취약점 분석
// 3. 학습 히트맵 구현
// 4. 실시간 진도 업데이트
```

### Phase 3: 개인화 및 최적화 (2일)
```javascript
// 1. 개인별 학습 추천 알고리즘
// 2. 적응형 난이도 조절
// 3. 학습 통계 분석
// 4. 성능 최적화
```

### 기술 스택
- **차트 라이브러리**: Recharts (레이더, 원형, 막대 차트)
- **애니메이션**: Framer Motion (부드러운 진도 변경 애니메이션)
- **아이콘**: Lucide React (일관된 아이콘 시스템)
- **상태 관리**: Zustand (진도 데이터 글로벌 상태)

---

## �📈 예상 결과

### 정량적 목표
- [ ] 문제 수 확장: 18개 → 200개 이상
- [ ] 과목 수 확장: 1개 → 6개
- [ ] 문제 유형 다양화: 객관식 → 객관식/코딩/주관식
- [ ] 관리자 문제 출제 시간: 평균 5분 이내
- [ ] **Python 학습 완주율: 85% 이상**
- [ ] **단계별 평균 점수: 80점 이상**

### 정성적 목표
- [ ] 교사의 자유로운 문제 출제 환경 제공
- [ ] 학생의 다양한 문제 유형 경험
- [ ] AI 기반 개인화된 피드백 제공
- [ ] 체계적인 학습 진도 관리
- [ ] **단계별 체계적 학습으로 학습 효과 극대화**
- [ ] **시각적 진도 확인으로 학습 동기 부여**

이 계획을 통해 현재의 단순한 문제 시스템을 **교육 현장에서 실제 활용 가능한 종합적인 문제 출제 플랫폼**으로 발전시킬 수 있습니다! 🎯
