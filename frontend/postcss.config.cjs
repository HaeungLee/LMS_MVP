//import tailwindcss from '@tailwindcss/postcss'; // 'tailwindcss'가 아니라 '@tailwindcss/postcss'를 import합니다.
//import autoprefixer from 'autoprefixer';

module.exports = {
  plugins: {
    '@tailwindcss/postcss': {}, // 패키지 이름을 문자열 키로 사용
    autoprefixer: {},
  },
};

